
赋值运算:
	a = b;			解释: 将b的值赋予a
	a += b			解释: 将b的值加上a的值然后赋予a

逻辑运算:
	a && b  			解释: a为真  并且 b为真
	a || b  			解释: a为真  或者 b为真

取成员:
	a.b			解释:取a的成员名为b的对象
	a.b()			解释:调用a的成员名为b的方法

显式转换:
	a = (int)b;			解释:将b强制转换为a, 如果类型不支持强制转换则引发异常

条件判断
	if (a == true){ a = false; }	解释:如果a为true,则将a的值变成false

未完待续..